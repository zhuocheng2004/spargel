group("default") {
  deps = [
    "//base",
    "//gpu",

    # tests
    "//base/memory:ref_count_ptr_test",

    # samples
    "//samples:demo",
    "//samples/vulkan_triangle",

    # modules
    "//modules/gpu",
    "//modules/window",
  ]
  if (is_macos) {
    deps += []
  }
}
